spring:
  application:
    name: tech-challenge
  datasource:
    url: jdbc:h2:mem:public
    username: sa
    password: password
  jpa:
    hibernate:
      ddl-auto: create
    database-platform: org.hibernate.dialect.H2Dialect